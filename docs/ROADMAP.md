# Bubot 로드맵

큰 방향만 적는다. 현재 위치는 `CURRENT.md`, 작업 상세는 `work/**/PLAN.md`.

1. **기준선** — 원본 저장소의 정리된 코드를 폴더 단위로 가져와 `main`이 빌드·기동되게 한다.
2. **정리 리팩터링** — TPM 이름 rename, Beta 제외 기능 `labs` 분리, 필요한 코드만 남기기, 로고 교체.
3. **Beta 배포** — 배포 대상 결정, `bootJar` 전환, `v0.1.0-beta` 태그·Release. Desktop·Mobile 시세·차트·호가·관심종목·계좌 Read-only.
4. **모의투자** — `labs`의 봇 매매 엔진을 paper trading으로 노출.
5. **정식** — 정식 출시 시 `beta` 브랜치 신설, `v1.0.0`.
